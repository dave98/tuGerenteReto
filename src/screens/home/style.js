import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginVertical: "10%",
        flex: 1,
        alignItems: "center",
        justifyContent: "flex-start",
    },
    usersContainer: {
        marginVertical: 15,
        width: "100%",
        borderColor: "#a2a2a2",
        borderWidth: 1,
    }
})