import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {
        flex: 1,
        width: "100%",
        justifyContent: "center",
        alignItems: "center",
    },
    message: {
        fontSize: 16,
        fontWeight: "300",
        letterSpacing: 1,
        color: "gray"
    }
})